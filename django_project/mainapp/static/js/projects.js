document.addEventListener('DOMContentLoaded', () => {
  const grid = document.querySelector('#projects .projects-grid');
  if (!grid) return;
  // Keep markup simple; backend can inject project items into this container
});
